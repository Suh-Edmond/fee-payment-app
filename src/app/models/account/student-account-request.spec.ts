import { StudentAccountRequest } from './student-account-request';

describe('StudentAccountRequest', () => {
  it('should create an instance', () => {
    expect(new StudentAccountRequest()).toBeTruthy();
  });
});
