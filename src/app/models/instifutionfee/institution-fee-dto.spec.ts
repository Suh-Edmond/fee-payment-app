import { InstitutionFeeDto } from './institution-fee-dto';

describe('InstitutionFeeDto', () => {
  it('should create an instance', () => {
    expect(new InstitutionFeeDto()).toBeTruthy();
  });
});
